Files:
gentests.py - Generates tests in 'tests' folder. Has two methods - easy() and hard(). The easy() method generates tests that test_sol.cpp can take.
compare.sh - compares the output of test_sol.cpp and sol.cpp on the easy tests
gen_output.sh - generates the output using sol.cpp

013 and 014 are manual tests

